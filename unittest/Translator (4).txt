void*j;
int b[];
int a[10000];
:int max(int a,int b){
	return a>b?a:b;
}
:int main(){
	int n,i,m;
	scanf("%d",&n);
	for(i=0;i<n;i++)
		scanf("%d",a+i);
	for(m=a[0],i=1;i<n;i++)
		m=max(m,a[i]);
	printf("max:%d\n",m);
	return 0;
}